interface FindResult<T> {
    store: T;
    params: Record<string, any>;
}
interface ParamNode<T> {
    paramName: string;
    store: T | null;
    inert: Node<T> | null;
}
interface Node<T> {
    part: string;
    store: T | null;
    inert: Map<number, Node<T>> | null;
    params: ParamNode<T> | null;
    wildcardStore: T | null;
}
declare class Memoirist<T> {
    root: Record<string, Node<T>>;
    history: [string, string, T][];
    private static regex;
    add(method: string, path: string, store: T): FindResult<T>['store'];
    find(method: string, url: string): FindResult<T> | null;
}

export { type FindResult, Memoirist, type Node, type ParamNode, Memoirist as default };
